# Frontend

The operator interface is planned but not implemented yet.

Keep browser application code in this directory once the backend API and device status model are defined. Do not commit generated dependencies or build output; document the chosen toolchain here when the frontend is started.